coverage:
  status:
    project:
      default:
        target: auto
        # The required 97% floor remains enforced by cargo-llvm-cov in CI.
        informational: true

comment:
  layout: "diff, files"
  behavior: default
  require_changes: false
  require_base: true
  require_head: true
  hide_project_coverage: false
