{
  "schemaVersion": 1,
  "knownMissBaseline": 2,
  "minimumRecall": 0.6,
  "cases": [
    {
      "name": "exact-match-remains-detectable",
      "path": "exact-match",
      "expectedDefinitions": 2,
      "expectedFindings": [
        {
          "rule": "duplicate-matcher",
          "matchers": ["the account is active", "the account is active"]
        }
      ]
    },
    {
      "name": "fuzzy-wording-with-varied-handler-shape",
      "path": "fuzzy-varied-handler",
      "expectedDefinitions": 2,
      "expectedCandidateSources": { "matcherBlocking": 1 },
      "expectedFindings": [
        {
          "rule": "near-duplicate-step",
          "matchers": ["I am on login page", "I am on the login page"]
        }
      ]
    },
    {
      "name": "direct-and-fluent-action-equivalence",
      "path": "direct-fluent-action",
      "expectedDefinitions": 2,
      "expectedCandidateSources": { "matcherBlocking": 1 },
      "expectedFindings": [
        {
          "rule": "near-duplicate-step",
          "matchers": ["I click the save button", "I click save button"]
        }
      ]
    },
    {
      "name": "typescript-path-alias-registration",
      "path": "path-alias",
      "expectedDefinitions": 0,
      "knownMisses": [
        {
          "id": "D-2",
          "rule": "duplicate-matcher",
          "matchers": ["aliased registration", "aliased registration"],
          "reason": "TypeScript compiler path aliases are not resolved yet"
        }
      ]
    },
    {
      "name": "cross-package-relative-reexport",
      "path": "cross-package",
      "expectedDefinitions": 2,
      "expectedFindings": [
        {
          "rule": "duplicate-matcher",
          "matchers": ["cross package registration", "cross package registration"]
        }
      ]
    },
    {
      "name": "local-registration-wrapper",
      "path": "local-wrapper",
      "expectedDefinitions": 0,
      "knownMisses": [
        {
          "id": "D-5",
          "rule": "duplicate-matcher",
          "matchers": ["wrapped registration", "wrapped registration"],
          "reason": "Single-level registration wrapper inference is not implemented yet"
        }
      ]
    },
    {
      "name": "polarity-conflict-is-intentionally-not-near",
      "path": "polarity",
      "expectedDefinitions": 2,
      "expectedAbsent": [
        {
          "rule": "near-duplicate-step",
          "matchers": ["the account is enabled", "the account is not enabled"]
        }
      ]
    }
  ]
}
