name: CukeDedup CodeQL configuration

queries:
  - uses: security-extended

paths-ignore:
  # Intentionally invalid JavaScript used to verify fail-closed parser diagnostics.
  - fixtures/corpus/malformed-matcher/steps.js
